#!/usr/bin/env groovy

def lvProjectPath = "source\\Jenkins.lvproj"
def lvBuildSpecName = "myBuildSpec"
def lvVersion = "2024Q1.0"
def lvBitness = "32"

lvPipeline(lvProjectPath, lvBuildSpecName, lvVersion, lvBitness)